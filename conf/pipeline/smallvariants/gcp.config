/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining per module options for execution of the pipeline on GCP Batch.
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/
process {
    withName: "^.*CRAM_CALL_GATK4:GATK4_HAPLOTYPECALLER\$" {
        machineType = 'template://gen-nfbatch-dev-medium'
        // n2-highmem-8, 100GB disk
        time        = { 16.h * task.attempt }
    }

    withName: '^.*GVCF_JOINT_GENOTYPE_GATK4:GATK4_GENOMICSDBIMPORT$' {
        machineType = 'template://gen-nfbatch-dev-medium'
        // n2-highmem-8, 100GB disk
        time        = { 16.h * task.attempt }
    }
}
