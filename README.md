# Nextflow CMGG Configs

Nextflow config files which can be included in the config for nf-cmgg pipelines.


## Styleguide
All config files should be linted using `nextflow lint`

```bash
nextflow lint -format -sort-declarations -spaces 4 -harshil-alignment -output concise .
```